# ThreeAI 模拟器 - 功能文档

**版本**: 0.3.2 · **最后更新**: 2026-05-29 · **状态**: ✅ 生产就绪

---

## 核心数据流

```
用户在游戏前设置面板配置
          ↓
setupPanelState 记录配置
          ↓
点击"开始游戏"
          ↓
activeConfig ← setupPanelState
gameState ← initGameStateFromConfig(activeConfig)
          ↓
游戏启动，UI 初始化
          ↓
每轮 gameLoop
          ↓
buildSystemPrompt() ← gameState （动态生成）
          ↓
AI 返回行为 (可能含 skill_changes / love_events / item_actions)
          ↓
各子系统自动处理 → gameState + activeConfig 同步更新
          ↓
UI 实时刷新（日志 / 卡片 / 编辑抽屉）
```

---

## 核心系统

| 功能模块 | 功能点 | 状态 |
|---------|-------|------|
| **动态配置架构** | GAME_CONFIG 全局模板 | ✅ |
| | activeConfig 游戏运行副本 | ✅ |
| | gameState 实时状态 | ✅ |
| | GAME_PRESETS 预设库（默认三玖 / 空白） | ✅ |
| | initGameStateFromConfig() 动态生成游戏状态 | ✅ |
| **AI Prompt** | buildSystemPrompt() 完全动态化 | ✅ |
| | 支持 1-5 个角色动态生成 | ✅ |
| | 支持 1-10 个房间动态生成 | ✅ |
| | actionType 6 态分类（AI 直接标注） | ✅ |
| | skill_changes 技能学习字段 | ✅ |
| | love_events 感情事件字段（可选） | ✅ |
| | purchases.dest 购物目标（inventory / 房间ID） | ✅ |
| | consume_items.from 消耗来源（inventory / 房间ID） | ✅ |
| | item_actions 物品互动字段（pick_up / put_down / give） | ✅ |
| | 职业行为规范段落 | ✅ |
| | 行为连续性规则 | ✅ |

---

## 游戏前设置面板

| 功能点 | 说明 | 状态 |
|-------|------|------|
| 全屏覆盖层 | 启动游戏前显示 | ✅ |
| 预设选择 | 默认三玖 / 空白配置 | ✅ |
| **角色配置** | 添加 / 编辑 / 删除（上限 5 个） | ✅ |
| | 字段：名字、颜色、性别、年龄、职业、月薪、性格、技能 | ✅ |
| | 初始背包物品（Enter 添加，点击标签删除） | ✅ |
| | 颜色选择器（`oninput` 双向实时联动 HEX 输入框） | ✅ |
| **房间配置** | 添加 / 编辑 / 删除（上限 10 个） | ✅ |
| | 字段：名字、描述、物品列表 | ✅ |
| **好感度设置** | 角色间关系度初始化（-100 ~ +100） | ✅ |
| | 2+ 角色时显示 | ✅ |
| **世界设定** | 场景名称（替换提示词中的"公寓"） | ✅ |
| | 故事背景自由文本 | ✅ |
| | 游戏开始时间（datetime-local） | ✅ |
| | NPC 配角列表 | ✅ |
| **API Key** | 输入框 + 状态指示器 | ✅ |
| | 自动同步到游戏控制面板 | ✅ |
| **存档读取** | 3 个本地槽位显示（时间 / 角色数 / 房间数 / 天数） | ✅ |
| | 读取存档到编辑器 | ✅ |
| | 删除存档（需确认） | ✅ |
| **存档导入** | 文件预览（角色 / 房间 / 游戏时间） | ✅ |
| | 确认导入 → 保存到 localStorage | ✅ |
| | 支持多存档汇总文件 | ✅ |
| | 支持单个存档文件 | ✅ |

---

## 游戏中编辑抽屉

| 功能点 | 说明 | 状态 |
|-------|------|------|
| 侧边栏滑出菜单 | 右侧，随时打开 | ✅ |
| **角色编辑** | 名字、颜色（实时同步 UI）、性别、年龄 | ✅ |
| | 添加 / 删除技能（点击标签删除） | ✅ |
| | 背包物品管理（手动添加 / 移除） | ✅ |
| | 关系度滑块（-100 ~ +100）实时调整 | ✅ |
| **房间编辑** | 修改房间描述 | ✅ |
| | 添加 / 删除物品 | ✅ |
| **世界观编辑** | 场景名称 / 故事背景实时修改 | ✅ |
| 修改立即生效 | 无需重启游戏 | ✅ |

---

## 颜色管理系统

| 功能点 | 说明 | 状态 |
|-------|------|------|
| setCharacterColor() 统一入口 | 同时更新 gameState + activeConfig + UI | ✅ |
| applyCharacterColors() | 动态注入 CSS 变量 | ✅ |
| data-color-id 属性方案 | 日志颜色动态跟随配置 | ✅ |
| 自定义角色颜色支持 | 不限角色数量 | ✅ |

---

## 存档系统

| 功能点 | 说明 | 状态 |
|-------|------|------|
| 3 个本地槽位 | localStorage 存储 | ✅ |
| 存档内容 | configSnapshot + gameState | ✅ |
| 导出全部存档 | JSON 格式，单文件 | ✅ |
| 导入存档 | 多存档汇总 / 单个存档自动识别 | ✅ |
| 格式兼容 | v1.0（仅 gameState）/ v2.0（configSnapshot） | ✅ |
| 游戏状态完整恢复 | 时间、天数、角色属性、睡眠状态、背包物品 | ✅ |

---

## 角色状态系统

| 功能点 | 说明 | 状态 |
|-------|------|------|
| 基础属性 | 精力、心情、饱腹感、卫生、钱包 | ✅ |
| **精力系统** | 被动恢复（卧室 20/h，浴室 8/h，其他 1/h） | ✅ |
| | 社交陪伴加速恢复 × 3 | ✅ |
| | 精力 < 10 紧急状态提示 | ✅ |
| | 三档处理规则（<10 / 10-30 / >30） | ✅ |
| **长期疲劳系统** | fatigueLevel = 连续工作天数 × 15 + 今日工时 × 3 | ✅ |
| | 每日零点重置 | ✅ |
| | 疲劳影响卧室恢复速度和能耗 | ✅ |
| | 高疲劳时行为智能选择（minimal / low） | ✅ |
| **睡眠系统** | 睡眠窗口：22:00 - 06:00 | ✅ |
| | 随机目标睡眠时长：4 - 9 小时 | ✅ |
| | 睡眠恢复 25 点精力/小时 | ✅ |
| | 睡眠不足惩罚（<5h: -40，5-7h: -20） | ✅ |
| | 被惊醒机制（大动静关键词检测） | ✅ |
| | 达到最长睡眠时长自动清醒 | ✅ |

---

## 工作行为系统

| 功能点 | 说明 | 状态 |
|-------|------|------|
| actionType 6 态 | primary_work / secondary_work / daily_life / leisure / rest / social | ✅ |
| 工作时长追踪 | 基于游戏时间间隔计算 | ✅ |
| 今日累计工时 | 日志输出（分钟 + 小时） | ✅ |
| 行为历史追踪 | actionHistory 保留最近 3 次 | ✅ |
| 比喻过滤 | isWorkRelatedAction() 清理"像...这样"等结构 | ✅ |
| 职业工作时段规范 | 惠舞 9-12/14-18，三玖 10-13/15-18 | ✅ |

---

## 技能学习系统

| 功能点 | 说明 | 状态 |
|-------|------|------|
| AI 返回 skill_changes | `{ learn: [], forget: [] }` 格式 | ✅ |
| applySkillChanges() | 自动同步到 gameState + activeConfig | ✅ |
| 技能标签 UI | 可点击删除 | ✅ |

---

## 角色物品系统

| 功能点 | 说明 | 状态 |
|-------|------|------|
| **角色背包** | `inventory[]` 字段，每个角色独立持有 | ✅ |
| | 初始背包通过 `initialInventory` 预设（设置面板可配置） | ✅ |
| | 旧存档自动兼容（缺失字段补 `[]`） | ✅ |
| **购物流向** | `purchases.dest = "inventory"` → 物品进背包 | ✅ |
| | `purchases.dest = 房间ID` → 物品进房间 | ✅ |
| | 旧格式 `purchases.room` 自动兼容 | ✅ |
| **物品流转** | `pick_up`：从房间取物 → 进背包 | ✅ |
| | `put_down`：从背包取物 → 放到当前/指定房间 | ✅ |
| | `give`：背包物品 → 赠给同房间角色 | ✅ |
| **物品消耗** | `consume_items.from = "inventory"` → 从背包消耗 | ✅ |
| | `consume_items.from = 房间ID` → 从房间消耗 | ✅ |
| | 房间找不到时自动兜底尝试背包 | ✅ |
| **UI 展示** | 角色卡片实时显示背包物品（橙色标签） | ✅ |
| | 游戏中抽屉：手动添加 / 移除背包物品 | ✅ |
| | 设置面板：配置初始背包物品 | ✅ |
| **存档** | 背包随存档完整保存 / 恢复 | ✅ |

### 物品流转规则表

| 操作 | 来源 | 目标 | AI 字段 |
|------|------|------|---------|
| 购买→背包 | 钱包 | 角色背包 | `purchases.dest = "inventory"` |
| 购买→房间 | 钱包 | 指定房间 | `purchases.dest = 房间ID` |
| 拾取 | 房间 | 角色背包 | `item_actions.type = "pick_up"` |
| 放下 | 角色背包 | 当前/指定房间 | `item_actions.type = "put_down"` |
| 赠与 | 角色背包 | 另一角色背包 | `item_actions.type = "give"` |
| 消耗（背包） | 角色背包 | 删除 | `consume_items.from = "inventory"` |
| 消耗（房间） | 指定房间 | 删除 | `consume_items.from = 房间ID` |

---

## 关系度系统

| 功能点 | 说明 | 状态 |
|-------|------|------|
| 角色间关系度 | 范围 -100（敌对）~ +100（亲密） | ✅ |
| 游戏前初始化 | setupPanelState.relationshipValues | ✅ |
| 游戏中实时调整 | 抽屉滑块（附恋爱状态徽章） | ✅ |
| 存档保留 | 读档后关系度完整恢复 | ✅ |
| 夜间关系结算 | 只评估有互动的配对，严格标准（寒暄=0，共鸣=±1/+2，冲突=-1/-2） | ✅ |
| 自然疏远衰减 | 无互动的配对每天 -1 | ✅ |
| 21 级关系标签 | getRelationshipLevel()，纯显示 | ✅ |

---

## 恋爱 / 婚姻系统

| 功能点 | 说明 | 状态 |
|-------|------|------|
| **关系状态** | 暧昧 / 恋人 / 已婚（gameState.coupleStatus） | ✅ |
| **触发方式** | AI 在日常行为中写出表白/求婚/分手场景 | ✅ |
| | 返回可选字段 love_events，系统解析后更新状态 | ✅ |
| **阈值验证** | 表白/接受：双方好感度 ≥ 65 | ✅ |
| | 求婚/结婚：双方 ≥ 82 且当前为恋人 | ✅ |
| | 分手/离婚需验证当前状态匹配 | ✅ |
| **AI 行为分层** | 暧昧：含蓄情感 / 恋人：可用"宝贝/亲爱的" / 已婚：可用"老公/老婆" | ✅ |
| **夜间倒退检查** | 已婚任一方 < 35 → 离婚；恋人 < 45 → 分手；暧昧 < 55 → 解除 | ✅ |
| **日志提示** | 💕 暧昧 / ❤️ 恋人 / 💍 结婚 / 💔 分手离婚 | ✅ |
| **UI 徽章** | 编辑抽屉关系滑块旁实时显示 | ✅ |
| **存档兼容** | coupleStatus 随存档保存/恢复，旧存档自动兼容 | ✅ |

---

## 生活日志系统

| 功能点 | 说明 | 状态 |
|-------|------|------|
| 日志类型样式 | system / thought / action / result / interaction / warning / error | ✅ |
| 角色颜色动态跟随 | 通过 data-color-id + CSS 变量 | ✅ |
| 自定义角色颜色支持 | 不限角色数量 | ✅ |
| 创作完成日志 | 作品标题（粉紫色）+ SD Prompt（青蓝色） | ✅ |
| CRT 扫描线效果 | ::after 伪元素，半透明纹理 | ✅ |

---

## 房间系统

| 功能点 | 说明 | 状态 |
|-------|------|------|
| 动态房间连接 | buildRoomConnections()，isHub 标记中心节点 | ✅ |
| 房间物品列表 | 可在游戏中增删 | ✅ |
| 房间精力恢复差异 | 卧室 20/h > 浴室 8/h > 其他 1/h | ✅ |

---

## 创作输出系统

| 功能点 | 说明 | 状态 |
|-------|------|------|
| workOutput 字段 | AI 完成创作时填写（title / type / description） | ✅ |
| generateArtworkImagePrompt() | 调用 DeepSeek 生成文生图命令词 | ✅ |
| 作品内容描述（不含创作环境） | 仅描述作品画面主体/构图/色彩，排除画师/画室/创作过程 | ✅ |
| Mock 离线支持 | 无 API Key 时返回预设 prompt | ✅ |
| 节流防重复 | 5 秒间隔限制 | ✅ |
| 作品提示词记录弹窗 | 一键导出全部记录 | ✅ |

---

## 游戏循环稳定性

| 功能点 | 说明 | 状态 |
|-------|------|------|
| try/catch/finally 保护 | gameLoop 主体异常自动恢复，`isProcessing` 保证重置 | ✅ |
| 多阶段 JSON 修复管线 | 直接解析 → repairJSONStrings → 位置截断 → 末尾截断 → Unterminated 修复 → 基础提取 | ✅ |
| interaction_with 类型兼容 | 数组/字符串均可，自动转换 | ✅ |
| 互动标题去重 | 同一对角色每轮只打印一次"进行了互动"标题 | ✅ |
| 独白过滤 | dialogue 全为同一说话人时跳过输出 | ✅ |

---



| 功能点 | 说明 | 状态 |
|-------|------|------|
| V4 Flash 模式 | deepseek-v4-flash，默认 | ✅ |
| V4 Pro 模式 | deepseek-v4-pro，旗舰 | ✅ |
| 思维链 - 默认 | THINKING_STYLE = 'default' | ✅ |
| 思维链 - 角色沉浸 | THINKING_STYLE = 'inner_os' | ✅ |
| 思维链 - 纯分析 | THINKING_STYLE = 'no_inner_os' | ✅ |

---

## Token 消耗参考

| 配置 | System | User | 总和 | 相对基准 |
|------|--------|------|------|---------|
| 默认（3角色） | ~300 | ~400 | ~700 | 基准 |
| 推荐（4角+8房） | ~380 | ~520 | ~900 | +28% ✅ |
| 最大（5角+10房） | ~450 | ~600 | ~1050 | +50% ⚠️ |

**限制速查**：角色上限 5 · 房间上限 10 · 预设上限 2 · Token 增量目标 <30%

---

## 关键函数速查

| 函数 | 作用 |
|------|------|
| `buildSystemPrompt()` | 动态生成 AI system prompt |
| `initGameStateFromConfig()` | 从配置初始化游戏状态 |
| `initSetupPanel()` | 初始化游戏前设置面板 |
| `startGameFromSetup()` | 从面板启动游戏 |
| `initEditDrawer()` | 初始化游戏中编辑抽屉 |
| `applySkillChanges()` | 处理技能变化 |
| `setCharacterColor()` | 统一颜色更新入口 |
| `applyCharacterColors()` | 动态注入角色颜色 CSS 变量 |
| `renderDrawerCharacters()` | 渲染角色编辑器 |
| `renderDrawerRooms()` | 渲染房间编辑器 |
| `addToInventory()` | 向角色背包添加物品（去重） |
| `removeFromInventory()` | 从角色背包移除物品 |
| `processItemAction()` | 处理 pick_up / put_down / give |
| `getRelationshipLevel()` | 返回 21 级关系标签（纯显示） |

---

## 默认角色一览

| 角色 | 职业 | 颜色 | 工作时段 |
|-----|------|------|---------|
| 惠舞 | 软件工程师 | 青蓝 #00d4ff | 9-12, 14-18 |
| 三玖 | 插画师 | 粉色 #ff79c6 | 10-13, 15-18 |
| 五月 | 美食博主 | 橙色 #ffb86c | 内容选题/拍摄/剪辑 |
